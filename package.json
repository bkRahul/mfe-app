{
  "name": "mfe-app",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "container": "cd container && yarn start",
    "marketing": "cd marketing && yarn start",
    "auth": "cd auth && yarn start",
    "dashboard": "cd dashboard && yarn start",
    "start": "concurrently \"yarn run container\" \"yarn run marketing\" \"yarn run auth\" \"yarn run dashboard\""
  },
  "devDependencies": {
    "concurrently": "^7.1.0"
  }
}
